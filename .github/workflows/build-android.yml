name: Build Android app

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/build-android.yml
      - src/**
      - static/**
      - src-tauri/**
      - package.json
      - bun.lock
      - svelte.config.js
      - tsconfig.json
      - vite.config.ts
      - postcss.config.js
      - tailwind.config.js

permissions:
  contents: write

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - name: Set up Rust with Android targets
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Cache Rust build files
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri -> target
          key: android

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android platform and build tools
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Android NDK
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r29
          link-to-sdk: true

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build optimized APK
        env:
          NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: bun run android:build -- --apk

      - name: Verify and prepare public Android package
        run: |
          mkdir -p release-assets
          apk="src-tauri/gen/android/app/build/outputs/apk/universal/release/app-universal-release.apk"
          if [ ! -f "$apk" ]; then
            echo "Tauri did not produce the expected universal Android APK: $apk" >&2
            exit 1
          fi

          "$ANDROID_HOME/build-tools/36.0.0/apksigner" verify --verbose "$apk"

          apk_size=$(stat --format=%s "$apk")
          max_apk_size=$((200 * 1024 * 1024))
          if [ "$apk_size" -gt "$max_apk_size" ]; then
            echo "Android APK is $apk_size bytes; native debug symbols may have been packaged." >&2
            exit 1
          fi

          echo "Prepared Android APK ($apk_size bytes)."
          cp "$apk" release-assets/holotrace-android-debug.apk

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: holotrace-android-${{ github.sha }}
          path: src-tauri/gen/android/app/build/outputs/apk/universal/release/app-universal-release.apk
          if-no-files-found: error
          retention-days: 14

      - name: Publish latest Android build
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: android-latest
          target_commitish: ${{ github.sha }}
          name: Latest Holotrace Android build
          body: |
            Automated development build from `main`.

            Source commit: `${{ github.sha }}`

            This universal APK uses Android debug signing and is intended for testing current development builds.
          prerelease: true
          make_latest: false
          files: release-assets/holotrace-android-debug.apk
          fail_on_unmatched_files: true
